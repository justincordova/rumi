PORT=3001
NODE_ENV=development
LOG_LEVEL=debug

# Postgres (local dev: docker compose up -d postgres)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/rumi

# Supabase (used to verify JWTs from the web client)
SUPABASE_URL=
SUPABASE_JWT_SECRET=
